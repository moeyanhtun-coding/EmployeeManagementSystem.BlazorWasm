@page "/logout"
@inject AuthenticationStateProvider AuthStateProvider

@code {
    protected async override Task OnInitializedAsync()
    {
        await((CustomAuthStateProvider)AuthStateProvider).MarkUserAsLogout();
        nav.NavigateTo("/login");
    }
}
